export enum RouteUrls {
  HOME = "/",
  HISTORY = "/history",
  CONTACTS = "/contacts",
  PARCELS = "/parcels",
  LOGIN = "/login",
}

export const urls = {
  baseUrl: "/",
};

export enum Page {
  HOME = "Главная",
  HISTORY = "История",
  CONTACTS = "Контакты",
  PARCELS = "Посылки",
}
