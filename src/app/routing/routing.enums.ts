export enum RouteUrls {
  HOME = "/",
  HISTORY = "/history",
  CONTACTS = "/contacts",
  LOGIN = "/login",
}

export const urls = {
  baseUrl: "/",
};

export enum Page {
  HOME = "Главная",
  HISTORY = "История",
  CONTACTS = "Контакты",
}
